<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rojo.ader.mascotas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mascotas">
        <activity
            android:name=".registro_user"
            android:exported="false"
            android:label="@string/title_activity_registro_user"
            android:theme="@style/Theme.Mascotas.NoActionBar" />
        <activity
            android:name=".mis_mascotas"
            android:exported="false"
            android:label="@string/title_activity_mis_mascotas"
            android:theme="@style/Theme.Mascotas.NoActionBar" />
        <activity
            android:name=".registro"
            android:exported="false"
            android:label="@string/title_activity_registro"
            android:theme="@style/Theme.Mascotas.NoActionBar" />
        <activity
            android:name=".menu"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>